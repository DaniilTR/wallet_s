# pubspec.yaml
name: secure_wallet
description: "Crypto Wallet Flutter App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  intl: ^0.19.0
  web3dart: ^2.7.3
  flutter_secure_storage: ^9.0.0
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/abi/erc20.json
